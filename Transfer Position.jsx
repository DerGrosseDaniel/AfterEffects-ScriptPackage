{    app.beginUndoGroup("Delete every second layer");        //Erde Layer 1 bis 926    //Papagei1 bis 575    compErde = null;    compPapagei = null;        for (var i = 1; i <= app.project.numItems; i++) {            if(app.project.item(i).name == "Erde"){                        compErde = app.project.item(i);                }    if(app.project.item(i).name == "Papagei"){                        compPapagei = app.project.item(i);                }    }                    for(i = 1; i <= 926; i++){        compErde.layer(i).transform.position.setValueAtTime(40,compPapagei.layer((i%575)+1).transform.position.valueAtTime(0,true))        }        app.endUndoGroup(); }