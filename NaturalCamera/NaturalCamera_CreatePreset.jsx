//create Presets//you have to select the camera first and the Point of interest second{        var selectedLayers = app.project.activeItem.selectedLayers;    var currentTime = app.project.activeItem.time;    var frameDuration = app.project.activeItem.frameDuration;    var data = ""        //get position of POI (this doesn't change)    data+=selectedLayers[0].transform.position.valueAtTime(currentTime,false)+"\n\n"    alert(data)        //save values of camera between inPoint and outPoint    cam = selectedLayers[1]    for (i = cam.inPoint; i<= cam.outPoint; i+=frameDuration){          data+= cam.transform.position.valueAtTime(i,false)+","          data+= cam.orientation.position.valueAtTime(i,false)+"\n"        }         // prompt to save file   var theFile = fileInputDialog("Save the text file.", "untitled.txt", "TEXT txt");      // if user didn't cancel...   if (theFile != null) {            // open file for "w"riting,      theFile.open("w","TEXT","????");            // write a line in the text file      theFile.writeln(data);            // if a comp is selected, write the name of all the layers      /*activeItem = app.project.activeItem;      if (activeItem != null && activeItem instanceof CompItem) {         for (x = 1; x <= activeItem.numLayers; ++x) {            theFile.writeln(activeItem.layer(x).name);         }      }*/         // close the text file      theFile.close();   }}